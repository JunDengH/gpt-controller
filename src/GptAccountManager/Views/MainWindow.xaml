<Window x:Class="GptAccountManager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:converters="clr-namespace:GptAccountManager.Converters"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="GPT Account Manager"
        Width="1280"
        Height="800"
        MinWidth="960"
        MinHeight="680"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        StateChanged="Window_StateChanged">
    <Window.Resources>
        <converters:AdaptiveSizeConverter x:Key="AdaptiveSizeConverter" />
        <converters:ResponsiveCardHeightConverter x:Key="ResponsiveCardHeightConverter" />
        <converters:ResponsiveCardWidthConverter x:Key="ResponsiveCardWidthConverter" />
        <converters:ViewportContentWidthConverter x:Key="ViewportContentWidthConverter" />

        <Style TargetType="Button"
               BasedOn="{StaticResource RoundedButtonStyle}">
            <Setter Property="MinHeight"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='40,44'}" />
            <Setter Property="FontSize"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}" />
        </Style>

        <Style x:Key="ResponsivePrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="MinHeight"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='40,44'}" />
            <Setter Property="FontSize"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}" />
        </Style>

        <Style x:Key="ResponsiveCardActionButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource CardActionButtonStyle}">
            <Setter Property="Height"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='36,42'}" />
            <Setter Property="MinHeight"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='36,42'}" />
            <Setter Property="FontSize"
                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='11,14'}" />
        </Style>

        <Style x:Key="ProviderActionLabelStyle" TargetType="TextBlock">
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDeepSeek}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="72"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            NonClientFrameEdges="None"
                            ResizeBorderThickness="7"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="WindowFrame"
            Background="{StaticResource BackgroundBrush}"
            BorderThickness="0"
            CornerRadius="0"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="72" />
                <RowDefinition Height="*" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Border x:Name="TitleBarFrame"
                    Grid.Row="0"
                    Background="{StaticResource TitleBarBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="22,0,12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Image Width="36"
                               Height="36"
                               Source="pack://application:,,,/Assets/AppIcon.png"
                               Stretch="Uniform" />
                        <TextBlock Margin="12,0,0,0"
                                   VerticalAlignment="Center"
                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='15,17'}"
                                   FontWeight="SemiBold"
                                   Text="GPT Account Manager" />
                    </StackPanel>

                    <Border Grid.Column="1"
                            Margin="0,0,12,0"
                            Padding="12,7"
                            VerticalAlignment="Center"
                            Background="{StaticResource SurfaceRaisedBrush}"
                            BorderBrush="{StaticResource BorderSubtleBrush}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11.5"
                                   Text="{Binding CurrentProviderText}" />
                    </Border>

                    <StackPanel Grid.Column="2"
                                 Orientation="Horizontal"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Click="Minimize_Click"
                                ToolTip="最小化">
                            <iconPacks:PackIconMaterial Width="18"
                                                        Height="18"
                                                        Kind="WindowMinimize" />
                        </Button>
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Click="MaximizeRestore_Click"
                                ToolTip="最大化或还原">
                            <iconPacks:PackIconMaterial x:Name="MaximizeIcon"
                                                        Width="18"
                                                        Height="18"
                                                        Kind="WindowMaximize" />
                        </Button>
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Click="Close_Click"
                                ToolTip="关闭">
                            <iconPacks:PackIconMaterial Width="18"
                                                        Height="18"
                                                        Kind="WindowClose" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="116" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <StackPanel Margin="0,54,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top">
                        <Border Width="58"
                                Height="58"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="29"
                                ToolTip="GPT Account Manager">
                            <Image Width="38"
                                   Height="38"
                                   Source="pack://application:,,,/Assets/AppIcon.png"
                                   Stretch="Uniform" />
                        </Border>

                        <Border Width="60"
                                Height="148"
                                Margin="0,44,0,0"
                                Padding="0,12"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="30">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0"
                                        Command="{Binding ShowAccountsCommand}"
                                        ToolTip="连接">
                                    <Button.Style>
                                        <Style TargetType="Button"
                                               BasedOn="{StaticResource NavIconButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAccountsPage}"
                                                             Value="True">
                                                    <Setter Property="Background" Value="#3F4043" />
                                                    <Setter Property="Effect"
                                                            Value="{StaticResource NavSelectionShadow}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <iconPacks:PackIconMaterial Width="23"
                                                                Height="23"
                                                                Kind="AccountOutline" />
                                </Button>

                                <Button Grid.Row="1"
                                        Command="{Binding ShowSettingsCommand}"
                                        ToolTip="设置">
                                    <Button.Style>
                                        <Style TargetType="Button"
                                               BasedOn="{StaticResource NavIconButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSettingsPage}"
                                                             Value="True">
                                                    <Setter Property="Background" Value="#3F4043" />
                                                    <Setter Property="Effect"
                                                            Value="{StaticResource NavSelectionShadow}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <iconPacks:PackIconMaterial Width="24"
                                                                Height="24"
                                                                Kind="CogOutline" />
                                </Button>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="1">
                    <Grid x:Name="AccountsPage">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAccountsPage}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid Margin="18,42,32,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='25,29'}"
                                               FontWeight="SemiBold"
                                               Text="连接管理" />
                                    <TextBlock Margin="0,8,0,0"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                               Text="统一管理 ChatGPT OAuth 账号与 DeepSeek Responses API" />
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='126,146'}"
                                            MinHeight="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='40,44'}"
                                            Margin="0,0,12,0"
                                            Command="{Binding RefreshAllCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Width="20"
                                                                        Height="20"
                                                                        Margin="0,0,8,0"
                                                                        Kind="Refresh" />
                                            <TextBlock VerticalAlignment="Center"
                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                                       Text="刷新全部" />
                                        </StackPanel>
                                    </Button>
                                    <Button Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='138,158'}"
                                             MinHeight="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='40,44'}"
                                             Click="AddConnection_Click"
                                             Style="{StaticResource ResponsivePrimaryButtonStyle}">
                                        <Button.ContextMenu>
                                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                <MenuItem Header="添加 ChatGPT OAuth"
                                                          Command="{Binding AddAccountCommand}" />
                                                <MenuItem Header="导入当前 ChatGPT"
                                                          Command="{Binding ImportCurrentCommand}" />
                                                <Separator />
                                                <MenuItem Header="{Binding DeepSeekMenuLabel}"
                                                          Command="{Binding ConfigureDeepSeekCommand}" />
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Width="21"
                                                                        Height="21"
                                                                        Margin="0,0,8,0"
                                                                        Kind="Plus" />
                                            <TextBlock VerticalAlignment="Center"
                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                                       Text="添加连接" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <ScrollViewer Grid.Row="1"
                                          Margin="0,44,0,0"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto">
                                <Grid>
                                    <ItemsControl Margin="-8,0,0,0"
                                                  VerticalAlignment="Top"
                                                  ItemsSource="{Binding Accounts}">
                                        <ItemsControl.Style>
                                            <Style TargetType="ItemsControl">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Accounts.Count}"
                                                                 Value="0">
                                                        <Setter Property="Visibility"
                                                                Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ItemsControl.Style>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel ItemWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource ResponsiveCardWidthConverter}}"
                                                           ItemHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=ScrollViewer}, Converter={StaticResource ResponsiveCardHeightConverter}}"
                                                           Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter">
                                                <Setter Property="HorizontalAlignment"
                                                        Value="Stretch" />
                                                <Setter Property="VerticalAlignment"
                                                        Value="Stretch" />
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="8">
                                                    <Border Background="{StaticResource SurfaceBrush}"
                                                            CornerRadius="20"
                                                            IsHitTestVisible="False">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Effect"
                                                                        Value="{StaticResource CardShadow}" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsActive}"
                                                                                 Value="True">
                                                                        <Setter Property="Effect"
                                                                                Value="{StaticResource ActiveCardShadow}" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>

                                                    <Border Padding="22,18,22,14"
                                                            Background="{StaticResource SurfaceBrush}"
                                                            CornerRadius="20">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="BorderBrush"
                                                                    Value="{StaticResource BorderBrush}" />
                                                            <Setter Property="BorderThickness"
                                                                    Value="1" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                             Value="True">
                                                                    <Setter Property="BorderBrush"
                                                                            Value="{StaticResource AccentBrush}" />
                                                                    <Setter Property="BorderThickness"
                                                                            Value="2" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>

                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>

                                                        <StackPanel Grid.Row="0"
                                                                    Orientation="Horizontal">
                                                            <Border Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='42,46'}"
                                                                    Height="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='42,46'}"
                                                                    Background="#696A6D"
                                                                    CornerRadius="23">
                                                                <iconPacks:PackIconMaterial Width="24"
                                                                                            Height="24"
                                                                                            HorizontalAlignment="Center"
                                                                                            VerticalAlignment="Center"
                                                                                            Foreground="#F1F1F2">
                                                                    <iconPacks:PackIconMaterial.Style>
                                                                        <Style TargetType="iconPacks:PackIconMaterial">
                                                                            <Setter Property="Kind" Value="AccountOutline" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsDeepSeek}" Value="True">
                                                                                    <Setter Property="Kind" Value="Api" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </iconPacks:PackIconMaterial.Style>
                                                                </iconPacks:PackIconMaterial>
                                                            </Border>
                                                            <TextBlock Margin="14,0,0,0"
                                                                       VerticalAlignment="Center"
                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='18,21'}"
                                                                       FontWeight="SemiBold"
                                                                       Text="{Binding Nickname}" />
                                                            <Border Margin="10,0,0,0"
                                                                    Padding="8,3"
                                                                    VerticalAlignment="Center"
                                                                    Background="#353638"
                                                                    CornerRadius="9">
                                                                <TextBlock Foreground="{StaticResource TextMutedBrush}"
                                                                           FontSize="10.5"
                                                                           Text="{Binding ProviderDisplayName}" />
                                                            </Border>
                                                        </StackPanel>

                                                        <Grid Grid.Row="1" Margin="0,14,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="28" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Grid Width="22"
                                                                  Height="22"
                                                                  HorizontalAlignment="Left">
                                                                <iconPacks:PackIconMaterial Width="19"
                                                                                            Height="19"
                                                                                            Foreground="{StaticResource AccentBrush}"
                                                                                            Kind="Check">
                                                                    <iconPacks:PackIconMaterial.Style>
                                                                        <Style TargetType="iconPacks:PackIconMaterial">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Collapsed" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Visible" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </iconPacks:PackIconMaterial.Style>
                                                                </iconPacks:PackIconMaterial>
                                                                <iconPacks:PackIconMaterial Width="19"
                                                                                            Height="19"
                                                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                                                            Kind="EmailOutline">
                                                                    <iconPacks:PackIconMaterial.Style>
                                                                        <Style TargetType="iconPacks:PackIconMaterial">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Visible" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Collapsed" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </iconPacks:PackIconMaterial.Style>
                                                                </iconPacks:PackIconMaterial>
                                                            </Grid>
                                                            <TextBlock Grid.Column="1"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,16'}"
                                                                       Text="{Binding Email}"
                                                                       ToolTip="{Binding Email}" />
                                                        </Grid>

                                                        <Grid Grid.Row="2" Margin="0,11,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="28" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <iconPacks:PackIconMaterial Width="19"
                                                                                        Height="19"
                                                                                        Foreground="{StaticResource TextSecondaryBrush}"
                                                                                        Kind="CrownOutline" />
                                                            <TextBlock Grid.Column="1"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,16'}"
                                                                       Text="{Binding PlanDisplayName}" />
                                                        </Grid>

                                                        <Grid Grid.Row="3" Margin="0,11,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="28" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Grid Width="21"
                                                                  Height="21"
                                                                  HorizontalAlignment="Left">
                                                                <iconPacks:PackIconMaterial Width="19"
                                                                                            Height="19"
                                                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                                                            Kind="AccountOutline">
                                                                    <iconPacks:PackIconMaterial.Style>
                                                                        <Style TargetType="iconPacks:PackIconMaterial">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Visible" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsOrganization}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Collapsed" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </iconPacks:PackIconMaterial.Style>
                                                                </iconPacks:PackIconMaterial>
                                                                <iconPacks:PackIconMaterial Width="19"
                                                                                            Height="19"
                                                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                                                            Kind="OfficeBuildingOutline">
                                                                    <iconPacks:PackIconMaterial.Style>
                                                                        <Style TargetType="iconPacks:PackIconMaterial">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Collapsed" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsOrganization}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Visible" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </iconPacks:PackIconMaterial.Style>
                                                                </iconPacks:PackIconMaterial>
                                                            </Grid>
                                                            <TextBlock Grid.Column="1"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,16'}"
                                                                       Text="{Binding OwnershipDisplayName}"
                                                                       ToolTip="{Binding OwnershipDisplayName}" />
                                                        </Grid>

                                                        <Border Grid.Row="4"
                                                                Height="1"
                                                                Margin="0,14,0,0"
                                                                Background="{StaticResource BorderSubtleBrush}" />

                                                        <Grid Grid.Row="5" Margin="0,13,0,0">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                            </Grid.RowDefinitions>

                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock VerticalAlignment="Center"
                                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,15'}"
                                                                           FontWeight="SemiBold"
                                                                           Text="{Binding MetricsTitle}" />
                                                                <Border Grid.Column="1"
                                                                        Padding="9,4"
                                                                        Background="#353638"
                                                                        CornerRadius="10">
                                                                    <TextBlock Foreground="{StaticResource TextMutedBrush}"
                                                                               FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='9.5,11'}"
                                                                               Text="{Binding QuotaStatusText}" />
                                                                </Border>
                                                            </Grid>

                                                            <Grid Grid.Row="1" Margin="0,9,0,0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="8" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>

                                                                <Border Padding="12,11"
                                                                        Background="{StaticResource SurfaceRaisedBrush}"
                                                                        BorderBrush="{StaticResource BorderSubtleBrush}"
                                                                        BorderThickness="1"
                                                                        CornerRadius="14">
                                                                    <Grid>
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                        </Grid.RowDefinitions>
                                                                        <StackPanel Orientation="Horizontal">
                                                                            <iconPacks:PackIconMaterial Width="15"
                                                                                                        Height="15"
                                                                                                        VerticalAlignment="Center"
                                                                                                        Foreground="{StaticResource AccentBrush}"
                                                                                                        Kind="TimerSand" />
                                                                            <TextBlock Margin="6,0,0,0"
                                                                                       VerticalAlignment="Center"
                                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                                                       Text="{Binding PrimaryMetricLabel}" />
                                                                        </StackPanel>
                                                                        <TextBlock Grid.Row="1"
                                                                                   Margin="0,4,0,0"
                                                                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='23,27'}"
                                                                                   FontWeight="SemiBold"
                                                                                   Text="{Binding FiveHourRemainingText}" />
                                                                        <ProgressBar Grid.Row="2"
                                                                                     Height="7"
                                                                                     Margin="0,7,0,0"
                                                                                     Foreground="{StaticResource AccentBrush}"
                                                                                     Maximum="100"
                                                                                     Value="{Binding FiveHourRemainingValue, Mode=OneWay}">
                                                                            <ProgressBar.Style>
                                                                                <Style TargetType="ProgressBar">
                                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding IsDeepSeek}" Value="True">
                                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </ProgressBar.Style>
                                                                        </ProgressBar>
                                                                        <TextBlock Grid.Row="3"
                                                                                   Margin="0,7,0,0"
                                                                                   Foreground="{StaticResource TextMutedBrush}"
                                                                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='9,10.5'}"
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding PrimaryMetricDetailText}"
                                                                                   Text="{Binding PrimaryMetricDetailText}" />
                                                                    </Grid>
                                                                </Border>

                                                                <Border Grid.Column="2"
                                                                        Padding="12,11"
                                                                        Background="{StaticResource SurfaceRaisedBrush}"
                                                                        BorderBrush="{StaticResource BorderSubtleBrush}"
                                                                        BorderThickness="1"
                                                                        CornerRadius="14">
                                                                    <Grid>
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" />
                                                                        </Grid.RowDefinitions>
                                                                        <StackPanel Orientation="Horizontal">
                                                                            <iconPacks:PackIconMaterial Width="15"
                                                                                                        Height="15"
                                                                                                        VerticalAlignment="Center"
                                                                                                        Foreground="#D4D5D8"
                                                                                                        Kind="CalendarWeek" />
                                                                            <TextBlock Margin="6,0,0,0"
                                                                                       VerticalAlignment="Center"
                                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                                                       Text="{Binding SecondaryMetricLabel}" />
                                                                        </StackPanel>
                                                                        <TextBlock Grid.Row="1"
                                                                                   Margin="0,4,0,0"
                                                                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='23,27'}"
                                                                                   FontWeight="SemiBold"
                                                                                   Text="{Binding WeeklyRemainingText}" />
                                                                        <ProgressBar Grid.Row="2"
                                                                                     Height="7"
                                                                                     Margin="0,7,0,0"
                                                                                     Foreground="#D4D5D8"
                                                                                     Maximum="100"
                                                                                     Value="{Binding WeeklyRemainingValue, Mode=OneWay}">
                                                                            <ProgressBar.Style>
                                                                                <Style TargetType="ProgressBar">
                                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding IsDeepSeek}" Value="True">
                                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </ProgressBar.Style>
                                                                        </ProgressBar>
                                                                        <TextBlock Grid.Row="3"
                                                                                   Margin="0,7,0,0"
                                                                                   Foreground="{StaticResource TextMutedBrush}"
                                                                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='9,10.5'}"
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding SecondaryMetricDetailText}"
                                                                                   Text="{Binding SecondaryMetricDetailText}" />
                                                                    </Grid>
                                                                </Border>
                                                            </Grid>

                                                            <Grid Grid.Row="2" Margin="2,9,2,0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <iconPacks:PackIconMaterial Width="14"
                                                                                            Height="14"
                                                                                            VerticalAlignment="Center"
                                                                                            Foreground="{StaticResource TextMutedBrush}"
                                                                                            Kind="ClockOutline" />
                                                                <TextBlock Grid.Column="1"
                                                                           Margin="6,0,0,0"
                                                                           VerticalAlignment="Center"
                                                                           Foreground="{StaticResource TextMutedBrush}"
                                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='9.5,11'}"
                                                                           Text="{Binding UpdatedLabel}" />
                                                                <TextBlock Grid.Column="2"
                                                                           VerticalAlignment="Center"
                                                                           Foreground="{StaticResource TextMutedBrush}"
                                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='9.5,11'}"
                                                                           Text="{Binding QuotaUpdatedValueText}" />
                                                            </Grid>
                                                        </Grid>

                                                        <Border Grid.Row="10"
                                                                Height="1"
                                                                Margin="0,9,0,0"
                                                                Background="{StaticResource BorderSubtleBrush}" />

                                                        <UniformGrid Grid.Row="11"
                                                                     Margin="-6,5,-6,-2"
                                                                     Rows="1">
                                                            <Grid>
                                                                <Button Command="{Binding DataContext.SwitchAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        CommandParameter="{Binding}"
                                                                        ToolTip="切换到此账号">
                                                                    <Button.Style>
                                                                        <Style TargetType="Button"
                                                                               BasedOn="{StaticResource ResponsiveCardActionButtonStyle}">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Visible" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Collapsed" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Button.Style>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <Border Width="28"
                                                                                Height="28"
                                                                                Margin="0,0,6,0"
                                                                                Background="#353638"
                                                                                CornerRadius="14">
                                                                            <iconPacks:PackIconMaterial Width="16"
                                                                                                        Height="16"
                                                                                                        HorizontalAlignment="Center"
                                                                                                        VerticalAlignment="Center"
                                                                                                        Kind="SwapHorizontal" />
                                                                        </Border>
                                                                        <TextBlock VerticalAlignment="Center"
                                                                                   Style="{StaticResource ProviderActionLabelStyle}"
                                                                                   Text="切换" />
                                                                    </StackPanel>
                                                                </Button>
                                                                <Button IsEnabled="False"
                                                                        ToolTip="当前登录账号">
                                                                    <Button.Style>
                                                                        <Style TargetType="Button"
                                                                               BasedOn="{StaticResource ResponsiveCardActionButtonStyle}">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Collapsed" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsActive}"
                                                                                             Value="True">
                                                                                    <Setter Property="Visibility"
                                                                                            Value="Visible" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Button.Style>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <Border Width="28"
                                                                                Height="28"
                                                                                Margin="0,0,6,0"
                                                                                Background="#626366"
                                                                                CornerRadius="14">
                                                                            <iconPacks:PackIconMaterial Width="16"
                                                                                                        Height="16"
                                                                                                        HorizontalAlignment="Center"
                                                                                                        VerticalAlignment="Center"
                                                                                                        Kind="Check" />
                                                                        </Border>
                                                                        <TextBlock VerticalAlignment="Center"
                                                                                   Style="{StaticResource ProviderActionLabelStyle}"
                                                                                   Text="当前" />
                                                                    </StackPanel>
                                                                </Button>
                                                            </Grid>

                                                            <Button Command="{Binding DataContext.RefreshAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Style="{StaticResource ResponsiveCardActionButtonStyle}"
                                                                    ToolTip="{Binding RefreshToolTip}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Border Width="28"
                                                                            Height="28"
                                                                            Margin="0,0,6,0"
                                                                            Background="#353638"
                                                                            CornerRadius="14">
                                                                        <iconPacks:PackIconMaterial Width="16"
                                                                                                    Height="16"
                                                                                                    HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"
                                                                                                    Kind="Refresh" />
                                                                    </Border>
                                                                    <TextBlock VerticalAlignment="Center">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Text"
                                                                                        Value="刷新" />
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsDeepSeek}"
                                                                                                 Value="True">
                                                                                        <Setter Property="Visibility"
                                                                                                Value="Collapsed" />
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding IsRefreshing}"
                                                                                                 Value="True">
                                                                                        <Setter Property="Text"
                                                                                                Value="刷新中…" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </StackPanel>
                                                            </Button>

                                                            <Button Command="{Binding DataContext.TestDeepSeekCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    ToolTip="发送最小 Responses API 测试请求">
                                                                <Button.Style>
                                                                    <Style TargetType="Button"
                                                                           BasedOn="{StaticResource ResponsiveCardActionButtonStyle}">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsDeepSeek}" Value="True">
                                                                                <Setter Property="Visibility" Value="Visible" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Border Width="28" Height="28" Margin="0,0,6,0"
                                                                            Background="#273E3A" CornerRadius="14">
                                                                        <iconPacks:PackIconMaterial Width="16" Height="16"
                                                                                                    HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"
                                                                                                    Foreground="#61D9C3"
                                                                                                    Kind="PlayOutline" />
                                                                    </Border>
                                                                    <TextBlock VerticalAlignment="Center"
                                                                               Style="{StaticResource ProviderActionLabelStyle}"
                                                                               Text="测试" />
                                                                </StackPanel>
                                                            </Button>

                                                            <Button Command="{Binding DataContext.RenameAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Style="{StaticResource ResponsiveCardActionButtonStyle}"
                                                                    ToolTip="{Binding EditToolTip}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Border Width="28"
                                                                            Height="28"
                                                                            Margin="0,0,6,0"
                                                                            Background="#353638"
                                                                            CornerRadius="14">
                                                                        <iconPacks:PackIconMaterial Width="16"
                                                                                                    Height="16"
                                                                                                    HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"
                                                                                                    Kind="PencilOutline" />
                                                                    </Border>
                                                                    <TextBlock VerticalAlignment="Center"
                                                                               Style="{StaticResource ProviderActionLabelStyle}"
                                                                               Text="编辑" />
                                                                </StackPanel>
                                                            </Button>

                                                            <Button Command="{Binding DataContext.DeleteAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    Foreground="{StaticResource DangerBrush}"
                                                                    IsEnabled="{Binding CanDelete}"
                                                                    Style="{StaticResource ResponsiveCardActionButtonStyle}"
                                                                    ToolTip="删除本地加密档案">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Border Width="28"
                                                                            Height="28"
                                                                            Margin="0,0,6,0"
                                                                            Background="#3A3032"
                                                                            CornerRadius="14">
                                                                        <iconPacks:PackIconMaterial Width="16"
                                                                                                    Height="16"
                                                                                                    HorizontalAlignment="Center"
                                                                                                    VerticalAlignment="Center"
                                                                                                    Kind="DeleteOutline" />
                                                                    </Border>
                                                                    <TextBlock VerticalAlignment="Center"
                                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                                               Style="{StaticResource ProviderActionLabelStyle}"
                                                                               Text="删除" />
                                                                </StackPanel>
                                                            </Button>
                                                        </UniformGrid>
                                                    </Grid>
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Border MinHeight="360"
                                            Margin="10"
                                            Padding="50"
                                            Background="{StaticResource SurfaceBrush}"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="18">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Accounts.Count}"
                                                                 Value="0">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                                            <Border Width="64"
                                                    Height="64"
                                                    HorizontalAlignment="Center"
                                                    Background="{StaticResource SurfaceRaisedBrush}"
                                                    CornerRadius="32">
                                                <iconPacks:PackIconMaterial Width="30"
                                                                            Height="30"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"
                                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                                            Kind="AccountPlusOutline" />
                                            </Border>
                                             <TextBlock Margin="0,20,0,0"
                                                        HorizontalAlignment="Center"
                                                        FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='19,22'}"
                                                        FontWeight="SemiBold"
                                                        Text="还没有连接" />
                                            <TextBlock Margin="0,9,0,24"
                                                       HorizontalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Text="添加 ChatGPT OAuth 账号，或配置 DeepSeek Responses API。" />
                                             <Button Click="AddConnection_Click"
                                                     Style="{StaticResource ResponsivePrimaryButtonStyle}">
                                                <Button.ContextMenu>
                                                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                        <MenuItem Header="添加 ChatGPT OAuth" Command="{Binding AddAccountCommand}" />
                                                        <MenuItem Header="导入当前 ChatGPT" Command="{Binding ImportCurrentCommand}" />
                                                        <Separator />
                                                        <MenuItem Header="{Binding DeepSeekMenuLabel}" Command="{Binding ConfigureDeepSeekCommand}" />
                                                    </ContextMenu>
                                                </Button.ContextMenu>
                                                <StackPanel Orientation="Horizontal">
                                                    <iconPacks:PackIconMaterial Width="20"
                                                                                Height="20"
                                                                                Margin="0,0,8,0"
                                                                                Kind="Plus" />
                                                    <TextBlock Text="添加第一个连接" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SettingsPage">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSettingsPage}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid Margin="18,42,32,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel>
                                <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='25,29'}"
                                           FontWeight="SemiBold"
                                           Text="设置" />
                                <TextBlock Margin="0,8,0,0"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                           Text="管理额度刷新频率和窗口行为" />
                            </StackPanel>

                            <ScrollViewer Grid.Row="1"
                                          Margin="0,36,0,0"
                                          HorizontalContentAlignment="Stretch"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto">
                                <StackPanel Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}, Converter={StaticResource ViewportContentWidthConverter}, ConverterParameter=18}"
                                            MaxWidth="1400"
                                            HorizontalAlignment="Left">
                                    <Grid>
                                        <Border Background="{StaticResource SurfaceBrush}"
                                                CornerRadius="20"
                                                Effect="{StaticResource CardShadow}"
                                                IsHitTestVisible="False" />
                                        <Border Padding="26"
                                                Background="{StaticResource SurfaceBrush}"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="20">
                                            <StackPanel>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Border Width="44"
                                                        Height="44"
                                                        Background="{StaticResource SurfaceRaisedBrush}"
                                                        CornerRadius="22">
                                                    <iconPacks:PackIconMaterial Width="21"
                                                                                Height="21"
                                                                                HorizontalAlignment="Center"
                                                                                VerticalAlignment="Center"
                                                                                Foreground="{StaticResource TextPrimaryBrush}"
                                                                                Kind="Refresh" />
                                                </Border>
                                                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                                    <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='15,17'}"
                                                               FontWeight="SemiBold"
                                                               Text="额度自动刷新" />
                                                    <TextBlock Margin="0,5,0,0"
                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                               FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                                               Text="按所选周期串行刷新所有已保存账号" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="2"
                                                           VerticalAlignment="Center"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,15'}"
                                                           FontWeight="SemiBold">
                                                    <Run Text="{Binding QuotaRefreshMinutes}" />
                                                    <Run Text=" 分钟" />
                                                </TextBlock>
                                            </Grid>

                                            <Slider Margin="4,24,4,0"
                                                    Minimum="5"
                                                    Maximum="120"
                                                    TickFrequency="5"
                                                    IsSnapToTickEnabled="True"
                                                    Value="{Binding QuotaRefreshMinutes}" />
                                            <Grid Margin="4,8,4,0">
                                                <TextBlock Foreground="{StaticResource TextMutedBrush}"
                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                           Text="5 分钟" />
                                                <TextBlock HorizontalAlignment="Right"
                                                           Foreground="{StaticResource TextMutedBrush}"
                                                           FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                           Text="120 分钟" />
                                            </Grid>
                                            </StackPanel>
                                        </Border>
                                    </Grid>

                                    <Grid Margin="0,16,0,0">
                                        <Border Background="{StaticResource SurfaceBrush}"
                                                CornerRadius="20"
                                                Effect="{StaticResource CardShadow}"
                                                IsHitTestVisible="False" />
                                        <Border Padding="26"
                                                Background="{StaticResource SurfaceBrush}"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="20">
                                            <StackPanel>
                                            <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='15,17'}"
                                                       FontWeight="SemiBold"
                                                       Text="窗口行为" />
                                            <TextBlock Margin="0,6,0,24"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='12,14'}"
                                                       Text="控制主窗口关闭和启动时的显示方式" />

                                            <CheckBox IsChecked="{Binding CloseToTray}"
                                                      Style="{StaticResource ToggleSwitchStyle}">
                                                <StackPanel>
                                                    <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,15'}"
                                                               Text="关闭窗口时最小化到托盘" />
                                                    <TextBlock Margin="0,4,0,0"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                               Text="仍可从托盘重新打开或退出应用" />
                                                </StackPanel>
                                            </CheckBox>

                                            <Border Height="1"
                                                    Margin="0,24"
                                                    Background="{StaticResource BorderSubtleBrush}" />

                                            <CheckBox IsChecked="{Binding StartMinimized}"
                                                      Style="{StaticResource ToggleSwitchStyle}">
                                                <StackPanel>
                                                    <TextBlock FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='13,15'}"
                                                               Text="启动后直接进入托盘" />
                                                    <TextBlock Margin="0,4,0,0"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}"
                                                               Text="下次启动应用时生效" />
                                                </StackPanel>
                                            </CheckBox>
                                            </StackPanel>
                                        </Border>
                                    </Grid>

                                    <Grid Margin="2,22,2,30">
                                        <TextBlock VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='10.5,12'}">
                                            <Run Text="GPT Account Manager v" /><Run Text="{Binding ApplicationVersion, Mode=OneWay}" /><Run Text=" · 本地优先 · 无遥测" />
                                        </TextBlock>
                                        <Button HorizontalAlignment="Right"
                                                Command="{Binding SaveSettingsCommand}"
                                                Style="{StaticResource ResponsivePrimaryButtonStyle}">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Width="20"
                                                                            Height="20"
                                                                            Margin="0,0,8,0"
                                                                            Kind="ContentSaveOutline" />
                                                <TextBlock Text="保存设置" />
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>

            <Border x:Name="FooterFrame"
                    Grid.Row="2"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0">
                <Grid Margin="30,0">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <iconPacks:PackIconMaterial Width="20"
                                                    Height="20"
                                                    Foreground="{StaticResource TextSecondaryBrush}"
                                                    Kind="ShieldCheckOutline" />
                        <TextBlock Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='11.5,13'}"
                                   Text="所有凭据仅保存在此设备上" />
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <ProgressBar Width="80"
                                     Height="4"
                                     Margin="0,0,12,0"
                                     IsIndeterminate="True">
                            <ProgressBar.Style>
                                <Style TargetType="ProgressBar"
                                       BasedOn="{StaticResource RoundedProgressBarStyle}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsWorking}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ProgressBar.Style>
                        </ProgressBar>
                        <TextBlock MaxWidth="560"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AdaptiveSizeConverter}, ConverterParameter='11,12.5'}"
                                   Text="{Binding StatusMessage}"
                                   ToolTip="{Binding StatusMessage}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
